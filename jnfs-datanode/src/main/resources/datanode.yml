server:
  port: 5369
  # 对外广播的 IP 地址，默认为本机局域网 IP
  # advertised_host: 192.168.1.100

storage:
  # 支持配置多个存储路径，用于扩容
  paths:
    - D:/data/jnfs/storage

# 注册中心配置
registry:
  host: localhost
  port: 5367
